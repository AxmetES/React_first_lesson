export interface TodoTask {
    userId: number,
    id: number,
    title: string,
    completed: boolean
}

export interface PhotoType {
    albumId: number,
    id: number,
    title: string,
    url: string,
    thumbnailUrl: string
}